$("#favorite_of_#{@animal.id}").html("#{j(render partial: 'favorites/favorite', locals: {animal: @animal, favorite: @favorite, favorites: @favorites})}")
-# $('#likes_buttons_<%= @animal.id %>').html("<%= j(render partial: 'likes/like', locals: {post: @post}) %>");
